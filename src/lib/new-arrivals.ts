// Общее число — сколько первых товаров из «Новинок» попадает в слайдер на главной (см.
// page.tsx). NewArrivalsManager.tsx показывает ту же границу, чтобы owner/admin видел, что
// «лишние» товары (без переставления повыше) в слайдер не влезут, а видны только по плитке
// «Новинки» в каталоге. Один и тот же список — просто на главной обрезан.
export const NEW_ARRIVALS_HOME_COUNT = 5;
